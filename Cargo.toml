[package]
name = "rgmin"
version = "0.2.0"
edition = "2024"
rust-version = "1.88"
license = "MIT"
description = "Local first-order minimization and Riemannian steppers."
repository = "https://github.com/OmniPotentRPC/rgmin"
readme = "README.md"
keywords = ["optimization", "conjugate-gradient", "linesearch"]
categories = ["science", "mathematics"]
authors = ["Rohit Goswami <rgoswami@ieee.org>"]

[lib]
name = "rgmin"
path = "src/lib.rs"
crate-type = ["lib", "cdylib", "staticlib"]

[features]
default = []
capi = []
par = ["dep:rayon"]
highs = ["dep:highs", "dep:highs-sys"]
slepc = ["dep:cc", "dep:pkg-config"]
primme = ["dep:cc", "dep:pkg-config"]
chase = ["dep:cc", "dep:pkg-config"]
libkrylov = ["dep:cc", "dep:pkg-config"]

[dependencies]
eindir-core = { version = "0.6.0", features = ["capi"] }
dlpk = { version = "0.1.5", features = ["ndarray"] }
ndarray = "0.17"
thiserror = "2"
rand = "0.9"
rayon = { version = "1.10", optional = true }
highs = { version = "2.1", optional = true }
highs-sys = { version = "1.12", optional = true }

[dev-dependencies]
approx = "0.5"
rgpot-core = "3.1.2"

[build-dependencies]
cbindgen = "0.29"
cc = { version = "1.2", optional = true }
pkg-config = { version = "0.3", optional = true }

[package.metadata.capi]
min_version = "0.10.17"

[package.metadata.capi.header]
name = "rgmin.h"
subdirectory = ""
generation = true
enabled = true

[package.metadata.capi.pkg_config]
name = "rgmin"
filename = "rgmin"
description = "Local first-order minimization C ABI"

[package.metadata.capi.library]
name = "rgmin"

[[example]]
name = "compare_highs"
required-features = ["highs"]

[[example]]
name = "ffi_throughput"
required-features = ["capi"]


[profile.release]
lto = "thin"
codegen-units = 1
